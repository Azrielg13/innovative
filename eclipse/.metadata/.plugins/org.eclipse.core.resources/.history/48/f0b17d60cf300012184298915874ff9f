package com.sce.esp.object.threads;

import java.sql.SQLException;

import com.sce.esp.object.ESPTestCase;
import com.sce.esp.object.model.Org;
import com.sce.esp.object.model.SceProj;

public class ThreadTest extends ESPTestCase{
	public void testSelectOrgs() throws SQLException{
		
//		for(int count = 0;count<100;count++)
		//getAll
		new Thread(new Runnable(){
			@Override
			public void run() {
				try {
					for(SceProj o: SceProj.getAll()){
						
//						try {
//							Thread.sleep(100);
//						} catch (InterruptedException e) {
//							e.printStackTrace();
//						}
						
					}
				} catch (SQLException e) {
					e.printStackTrace();
				}
			}
			
		}).run();
		
		//getAllActive
		new Thread(new Runnable(){
			@Override
			public void run() {
				try {
					SceProj.getAllActive();
				} catch (SQLException e) {
					e.printStackTrace();
				}
			}
			
		}).run();
			
	}
}
