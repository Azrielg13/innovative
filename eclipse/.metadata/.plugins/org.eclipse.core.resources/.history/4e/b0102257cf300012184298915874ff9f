package com.sce.esp.object.model;

import java.sql.SQLException;
import java.util.Collection;

import com.sce.esp.object.ESPTestCase;

public class CktTieTest extends ESPTestCase{
	public void testDoubleDB() throws SQLException{
		Collection<CktTie> all = CktTie.getAll(); 
		assertNotSame(0,all.size());
		assertNotNull(all.iterator().next().getFromCkt());
	}
}
