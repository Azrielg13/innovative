package com.sce.esp.notif;
import com.sce.esp.dialog.NewUserDialog;
import com.sce.esp.object.ESPTestCase;
import com.sce.esp.object.model.Department;
import com.sce.esp.object.model.User;

public class newUserNotifTest extends ESPTestCase {

	protected void setUp() throws Exception {
		super.setUp();
		Department.getActiveInstance().getSyss();
	}
	
	
	public void testNewUserLotusNote(){
//		LotusNotification.sendNewUserNotif(User.getActiveUser());
//		LotusNotification.sendTest(User.getActiveUser()); test
		new NewUserDialog(null, User.getActiveUser());
	}
	//test by kate
}