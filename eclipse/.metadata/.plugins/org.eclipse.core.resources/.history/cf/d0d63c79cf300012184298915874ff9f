package com.sce.esp.notif;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Statement;

import com.sce.esp.log.EspLogger;
import com.sce.esp.object.dao.SubDailyDAO;
import com.sce.esp.object.jpa.EntityManagerHelper;
import com.sce.esp.util.ESPProperties;

public class logTest {

	/**
	 * @param args
	 * @throws SQLException 
	 */
	public static void main(String[] args) throws SQLException {
		
//		try {
//			EspLogger.init(false, EspLogger.LEVEL.MESSAGE);
//			EntityManagerHelper.init(ESPProperties.getInstance().getProdDbUrl(), "gonzalf1", "S3cret#3");
//			EntityManagerHelper.init(ESPProperties.getInstance().getGusDbUrl(), "suwannn", "edison");
//		} catch (Exception e) {
//			e.printStackTrace();
//		}
//			
//			System.out.println(SubDailyDAO.getPspActive().size());
//		"SELECT COUNT(*) FROM SubDaily o where o.insert_ts >\'31-may-10\'"

		
		try {
			String connectionURL = "jdbc:oracle:thin:@ORAP066.sce.com:1526:P066";
			Connection connection = null;
			Statement statement = null;
			ResultSet rs = null;
			connection = DriverManager.getConnection(connectionURL, "suwannn", "edison1");
			statement = connection.createStatement();
			String QueryString = "SELECT COUNT(*) FROM MDI240_CKT_DAILY where insert_ts >\'31-may-12\'";
			rs = statement.executeQuery(QueryString);
			while (rs.next()) {
				System.out.println(rs.getInt(1)+"\n");
			}
			rs.close();
			statement.close();
			connection.close();
		} 
		catch (Exception ex) {
			System.out.println("Unable to connect to batabase.");
		}
	}

}
